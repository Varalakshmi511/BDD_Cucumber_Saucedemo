//name=PropertiesConfig
#created variable to store logs in logs folder
property.filename = logs
#logs destination, it will be stored in console and file both
appenders = console, file 


appender.console.type = Console
appender.console.name = STDOUT
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = [%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n


appender.file.type = File
appender.file.name = LOGFILE
appender.file.fileName=${filename}/mylog.log
appender.file.layout.type=PatternLayout
appender.file.layout.pattern=[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n


loggers=file
#in below you have to give the stepdefinition class name
logger.file.name=StepDef
#we have off,fatal, error, warn, info, debug, trace, all- if you give all then only all above eligible, if debug only above debug is applicable not below(trac
logger.file.level = debug
logger.file.appenderRefs = file
logger.file.appenderRef.file.ref = LOGFILE

rootLogger.level = off
rootLogger.appenderRefs = stdout
rootLogger.appenderRef.stdout.ref = STDOUT